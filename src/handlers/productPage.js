/**
 * Handler for page /products/page/:categoryId/:productId/:tab
 * @param currentSession
 * @param params
 * @param query
 * @returns {string}
 */
export const productPage = (currentSession, params, query) => {
  console.log({ currentSession, params, query });
  return '';
};
